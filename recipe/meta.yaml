{% set name = "mamba" %}
{% set version = "0.2.0" %}
{% set sha256 = "79bd335aab743cea7360d859afcdb7b29bcf84f771ca51c111478cc4f88e7571" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/QuantStack/mamba/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - win_cpp14_compat.patch  # [win]

build:
  number: 0
  skip: true  # [win and vc<14]
  entry_points:
    - mamba = mamba.mamba:main

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - setuptools
    - python
    - pip
    - pybind11
    - conda >=4.8
    - libsolv >=0.7.11
    - libcurl
    - openssl
    - libarchive
    - nlohmann_json
  run:
    - python
    - conda >=4.7.12

test:
  imports:
    - mamba
    - mamba.mamba_api
  commands:
    - mamba --help
    # these tests work when run on win, but for some reason not during conda build
    - mamba create -n test_py2 python=2.7 --dry-run
    - mamba install xtensor xsimd -c conda-forge --dry-run

about:
  home: https://github.com/QuantStack/mamba
  license: BSD 3-Clause
  license_file: LICENSE
  license_family: BSD
  summary: 'A fast drop-in alternative to conda, using libsolv for dependency resolution'
  description: 'A fast drop-in alternative to conda, using libsolv for dependency resolution'
  dev_url: https://github.com/QuantStack/mamba

extra:
  recipe-maintainers:
    - SylvainCorlay
    - JohanMabille
    - wolfv
