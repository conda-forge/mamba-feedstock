{% set name = "mamba" %}
{% set version = "0.6.0" %}
{% set sha256 = "229b621bdb6bf48f3b67c87a66ed5f450fc48efc1d7ea235c45202487b5a4872" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/TheSnakePit/mamba/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: true  # [win and vc<14]
  entry_points:
    - mamba = mamba.mamba:main

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - setuptools
    - python
    - pip
    - pybind11
    - conda >=4.8
    - libsolv >=0.7.14
    - libcurl
    - openssl
    - libarchive
    - libiconv
    - nlohmann_json
    - cpp-filesystem
  run:
    - python
    - conda >=4.8

test:
  imports:
    - mamba
    - mamba.mamba_api
  commands:
    - mamba --help
    # these tests work when run on win, but for some reason not during conda build
    - mamba create -n test_py2 python=2.7 --dry-run  # [linux]
    - mamba install xtensor xsimd -c conda-forge --dry-run  # [linux and x86_64]

about:
  home: https://github.com/TheSnakePit/mamba
  license: BSD-3-Clause
  license_file:
    - LICENSE
    - CPP_FILESYSTEM_LICENSE.txt
  license_family: BSD
  summary: A fast drop-in alternative to conda, using libsolv for dependency resolution
  description: A fast drop-in alternative to conda, using libsolv for dependency resolution
  dev_url: https://github.com/TheSnakePit/mamba

extra:
  recipe-maintainers:
    - SylvainCorlay
    - JohanMabille
    - wolfv
    - ericmjl
